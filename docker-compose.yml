services:
  sentinel:
    build: .
    container_name: dayz-sentinel
    ports:
      - "${API_PORT:-8000}:8000"
    volumes:
      - ./sentinel_spr019/database/sqlite:/app/sentinel_spr019/database/sqlite
    restart: unless-stopped
