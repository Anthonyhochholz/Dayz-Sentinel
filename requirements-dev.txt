# Development and test dependencies.
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
# httpx backs starlette.testclient.TestClient, used by tests/.
httpx==0.28.1
# requests backs sentinel_spr019/scripts/test_api.py (manual endpoint runner).
requests==2.33.1
